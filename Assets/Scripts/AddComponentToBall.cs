using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddComponentToBall : MonoBehaviour
{
    MapColorChangeOnCollision mapColor;
    void Awake()
    {
       // GameObject ball = GameObject.FindGameObjectWithTag("Player");
        //if (ball != null) ball.AddComponent<MapColorChangeOnCollision>();
    }
}
